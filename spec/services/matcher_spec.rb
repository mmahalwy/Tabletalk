RSpec.describe 'Matcher', type: :model do
  # let(:city) { create(:city) }
  # let(:week) { create(:week) }
  # let(:user1) { create(:user, city: city) }
  # let(:user2) { create(:user, city: city) }
  # let(:user3) { create(:user, city: city) }
  # let(:user4) { create(:user, city: city) }

  # let(:confirmat1) { create(:confirmat, user: user1, week: week) }
  # let(:confirmat2) { create(:confirmat, user: user2, week: week) }
  # let(:confirmat3) { create(:confirmat, user: user3, week: week) }
  # subject { described_class.new(city, week) }

  it "returns confirmed users for the week" do
    # expect(subject.confirmed_users).to have_length(3)
  end
end
